Forkk's Common NixOS Configuration
==================================

This is a git repository which contains a set of common Nix expression used to
construct my various system configurations across multiple systems.
